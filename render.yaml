services:
  - type: web_service
    name: complainsetu-backend
    env: node
    plan: free
    root: backend
    buildCommand: npm install
    startCommand: npm start

  - type: static_site
    name: complainsetu-frontend
    env: static
    plan: free
    root: .
    buildCommand: npm install && npm run build
    publishPath: dist
